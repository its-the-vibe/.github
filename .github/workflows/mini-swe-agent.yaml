name: "Reusable Mini-SWE-Agent"

on:
  workflow_call:
    inputs:
      issue_url:
        required: true
        type: string
      issue_number:
        required: true
        type: number
      issue_title:
        required: true
        type: string
    secrets:
      PREPAY_GEMINI_API_KEY:
        required: true

jobs:
  run-agent:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - uses: actions/setup-go@v7
        with:
            go-version: "1.27"
            check-latest: true

      - name: Download dependencies
        run: go mod download | echo "no go.mod found, skipping"

      - name: Install mini-swe-agent
        run: |
          pip install mini-swe-agent

      - name: Run mini-swe-agent
        env:
          GEMINI_API_KEY: ${{ secrets.PREPAY_GEMINI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mini-swe-agent \
            --yolo \
            --model "gemini/gemini-flash-latest" \
            --task "${{ inputs.issue_url }}" \
            --exit-immediately

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "fix: resolved issue #${{ inputs.issue_number }} via mini-swe-agent"
          # Issue title incorporated in PR title:
          title: "Fix #${{ inputs.issue_number }}: ${{ inputs.issue_title }}"
          body: |
            Automated fix generated by `mini-swe-agent` for issue #${{ inputs.issue_number }}.

            Closes #${{ inputs.issue_number }}
          branch: "mini-swe-agent/issue-${{ inputs.issue_number }}"
          delete-branch: true